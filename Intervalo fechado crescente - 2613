#include <stdio.h>

int main() {
	int a, b, i;
	scanf(" %d", &a);
	scanf(" %d", &b);
	
	if(a == b){
        printf("%d\n", a);	
        return 0;
	}
    else{
	for (i = a; i <= b; i++){
		printf("%d\n", i);

	}
    }
	return 0;
}
